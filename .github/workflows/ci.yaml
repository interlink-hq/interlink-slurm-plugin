# Dagger-based CI jobs are disabled. Use the K3s-based e2e workflow
# .github/workflows/integration-test-k3s.yaml) instead.
name: integration-tests

on:
  push:
    #branches: [main,next,next2next]
  pull_request:

jobs:
  placeholder:
    name: dagger-disabled
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - run: echo "Dagger-based integration tests are disabled. See integration-test-k3s.yaml."
